/**
 * TODO(M10): the learner's agent-run history. Registered now so the route, its guard and
 * the links from the agent exercise can exist before the list does.
 */
export function RunsPage() {
  return (
    <section className="py-12">
      <h1 className="text-2xl font-semibold tracking-tight">Agent runs</h1>
      <p className="mt-3 text-sm text-slate-500">This screen arrives in milestone M10.</p>
    </section>
  );
}
