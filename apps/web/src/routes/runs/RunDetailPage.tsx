/**
 * TODO(M10): the trace viewer, shared by Module 5 (server loop), Module 6 (the learner's
 * own loop) and the SRE lesson. One viewer for all three is the point.
 */
export function RunDetailPage() {
  return (
    <section className="py-12">
      <h1 className="text-2xl font-semibold tracking-tight">Run trace</h1>
      <p className="mt-3 text-sm text-slate-500">This screen arrives in milestone M10.</p>
    </section>
  );
}
