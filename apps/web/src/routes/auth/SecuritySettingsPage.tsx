/**
 * TODO(M6): implemented in milestone M6 (auth + MFA UI). The route is registered in
 * App.tsx already so the guard and navigation can be wired and tested before the form exists.
 */
export function SecuritySettingsPage() {
  return (
    <section className="mx-auto max-w-md py-12">
      <h1 className="text-2xl font-semibold tracking-tight">Security settings</h1>
      <p className="mt-3 text-sm text-slate-500">This screen arrives in milestone M6.</p>
    </section>
  );
}
